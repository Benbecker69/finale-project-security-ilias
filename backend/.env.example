# Copy to .env and adjust. (On the vulnerable branch a filled .env is committed on purpose.)
PORT=4000
NODE_ENV=development
JWT_SECRET=change_me
JWT_EXPIRES_IN=
STRIPE_SECRET_KEY=
ADMIN_BOOTSTRAP_PASSWORD=Admin123!
CORS_ORIGIN=*
